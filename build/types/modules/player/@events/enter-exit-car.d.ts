import { type PlayerMp } from "..";
import { type VehicleSeatsEnum } from "@/shared/enums";
import { type VehicleMp } from "../../vehicle";
export declare function putInVehicleWithEvent(player: PlayerMp, vehicle: VehicleMp, seat: VehicleSeatsEnum): boolean;
