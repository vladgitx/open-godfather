export type OpenConfig = {
    name: string
    language: string
    website: string
    map: string
    mode: string
    stuntBonuses: boolean
    nametagDrawDistance: number
    hour: number
    weather: number
}